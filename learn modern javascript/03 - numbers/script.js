// console.log(Math)

// console.log(Math.PI)

// console.log(Math.E)

const x = 7.7

console.log(Math.round(x)) // Returns a supplied numeric expression rounded to the nearest integer.

console.log(Math.floor(x)) // Returns the greatest integer less than or equal to its numeric argument.

console.log(Math.ceil(x)) // Returns the smallest integer greater than or equal to its numeric argument.

console.log(Math.trunc(x)) // Returns the integral part of the a numeric expression, x, removing any fractional digits. If x is already an integer, the result is x.

